language: node_js

node_js:
  - "stable"

services:
  - postgresql

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - npm run undo:tests
  - npm run db:migrate

after_success:
  - npm run coveralls
  - npm run delete-dist

